# Archived — not part of production Blyp

This package is orphaned scaffolding (Expo ~48 / React Native 0.71). The shipped app does not import it.

Do not install, audit, or deploy this package as part of a release until it is rewritten against the current Expo/React Native stack and wired explicitly.
