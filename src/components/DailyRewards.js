// Daily rewards are intentionally unavailable until the backend owns eligibility,
// abuse controls, idempotent grants, streak state, and the resulting ledger entry.
const DailyRewards = () => null;

export default DailyRewards;
