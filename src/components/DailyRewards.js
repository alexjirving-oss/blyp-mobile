/**
 * Daily rewards UI is disabled until server-authoritative grant exists.
 * Kept as a no-op so stale imports fail closed instead of inviting client mint.
 */
export default function DailyRewards() {
  return null;
}
